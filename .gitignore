node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# Runtime state — the ledger is an audit trail of a *running* system,
# not source. A sample lives in docs/ instead.
.headcount/

# Local probe scripts, not part of the project
.scratch/
